#!/bin/bash

# Workaround because of <https://github.com/nvim-telescope/telescope.nvim/issues/2517>

git log --date=short --pretty="format:%h %ad %s%d [%an]" "$1" >/tmp/gitlog && echo "" >>/tmp/gitlog && cat /tmp/gitlog
