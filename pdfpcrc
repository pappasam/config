#### Softlink to ~/.config/pdfpc/pdfpcrc

# Navigation
bind k         prevSeen
bind j         nextUnseen
bind f         goto
bind g         gotoFirst
bind G         gotoLast

# Jumps
bind Tab       overview
bind BackSpace histBack

# Exit (only use system exit; I don't like accidentally quitting)
unbind Escape
unbind q

# Timer related
bind s         start
bind p         pause
bind r         resetTimer
bind R         reset

# Presentation properties
bind n         note
bind o         overlay
bind e         endSlide
bind S+s       lastSlide
bind S+r       jumpLastSlide
